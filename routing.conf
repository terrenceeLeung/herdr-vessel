# herdr-vessel 路由边表（机器可读，插件与文档的唯一拓扑真相源）
#
# 格式: <from> -> <to>        （# 开头为注释，空行忽略）
#
# 本文件只列【回边】（返工边）——hop 计数只数这些。
# 未列出的合法边（fm->ce、ce->reviewer、任何->captain）视为正向，只审计不计数。
# 改拓扑 = 改这里 + 同步 TEAM.md 的人类可读表格。

reviewer -> chief-engineer
chief-engineer -> first-mate
reviewer -> first-mate
